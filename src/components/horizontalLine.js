import React from "react";

const ColoredLine = ({ color }) => (
    <hr
        style={{
            color: "#1a66ff",
            border : "solid"
        }}
    />
);


<ColoredLine color="red" />

export default ColoredLine;